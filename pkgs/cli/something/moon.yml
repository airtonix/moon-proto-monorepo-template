$schema: "https://moonrepo.dev/schemas/project.json"

language: go

tasks:
  lint:
    command: "go vet ./..."
  build:
    command: "go build ./..."
  test:
    command: "go test ./..."
  publish:
    command: |
      TAG="${1:-latest}"
      echo "TODO: publish something-cli release artifacts with tag ${TAG}"
